# heroku login
# heroku git:clone -a adaptive-chatbot

git add .
git commit -m "make it better"

# git remote rm origin
# git remote add origin https://github.com/AdaptiveCB/chatbot-adaptativo.git

git push origin master